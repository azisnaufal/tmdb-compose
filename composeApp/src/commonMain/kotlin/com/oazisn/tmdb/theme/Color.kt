package com.oazisn.tmdb.theme

import androidx.compose.ui.graphics.Color

object NetflixColors {
    val RedPrimary = Color(0xFFE50914)
    val RedHover = Color(0xFFF40612)
    val RedMuted = Color(0xFFE50914).copy(alpha = 0.6f)

    val BlackCanvas = Color(0xFF000000)
    val GrayDark = Color(0xFF141414)
    val GraySurface = Color(0xFF1A1A1A)
    val GrayMedium = Color(0xFF333333)
    val GrayPlaceholder = Color(0xFF4D4D4D)
    val GrayMuted = Color(0xFF808080)
    val GrayDim = Color(0xFF999999)
    val GrayHover = Color(0xFFB3B3B3)

    val WhitePrimary = Color(0xFFFFFFFF)
    val WhiteOverlay = Color(0xFFFFFFFF).copy(alpha = 0.7f)

    val OverlayDark = Color(0xFF000000).copy(alpha = 0.7f)

    val Success = Color(0xFF46D369)
    val StarRating = Color(0xFFF5C518)
}
